# Navigation Patterns

> ナビゲーションの構成パターン。

---

## サイドバーナビゲーション

メインのナビゲーション。サイドバー内に配置。

> 仕様・HTMLサンプル・状態・アクセシビリティは `components/sidebar.md` を参照。

---

## パンくずリスト

現在のページの階層構造を示す。仕様・HTMLサンプル・アクセシビリティは `components/breadcrumb.md` を参照。

---

## タブナビゲーション

同一ページ内のセクション切り替え。仕様・HTMLサンプル・アクセシビリティは `components/tabs.md` を参照。

---

## 禁止パターン

| 禁止 | 理由 | 代替 |
|------|------|------|
| ナビにダークモード背景 | メインとのコントラストが強すぎる | `bg-white` + ボーダー |
| アクティブ状態が色のみ | アクセシビリティ違反 | 背景色 + ボーダー or アイコン変化 |
| 3階層以上のネストナビ | 複雑すぎて迷う | 2階層まで |
| `aria-label` なしの `<nav>` | ランドマーク一覧で複数ナビを区別できない | `aria-label="メインナビゲーション"`（or `aria-labelledby`）を付与 <!-- A11Y_NAV_ARIA_LABEL_REQUIRED --> |
