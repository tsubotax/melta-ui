# Typography

> フォントスケール、行間・字間の設計。日本語 UI の一般的な組版指針をベースに、可読性を重視して独自に拡張。

---

## フォントファミリー

```
font-sans: "Inter", "Hiragino Sans", "Hiragino Kaku Gothic ProN", "Noto Sans JP", sans-serif
font-mono: "JetBrains Mono", "SF Mono", monospace
```

Tailwind CSS 4のデフォルト`font-sans`を使用。日本語環境ではシステムフォントにフォールバック。

> プロジェクト固有のフォントは `foundations/theme.md` の Font Stack で上書き可能。

---

## フォントサイズスケール

| Level | Tailwindクラス | サイズ | 行間 | 字間 | 用途 |
|-------|---------------|--------|------|------|------|
| H1 | `text-3xl font-bold` | 32px (2rem) | 140% | 1% | ページタイトル |
| H2 | `text-2xl font-semibold` | 26px (1.625rem) | 140% | 1% | セクション見出し |
| H3 | `text-xl font-semibold` | 22px (1.375rem) | 140% | 1% | カード見出し |
| H4 | `text-lg font-medium` | 20px (1.25rem) | 150% | 1% | サブ見出し |
| Body | `text-base` | 18px (1.125rem) | 200% | 2% | 本文（ベース） |
| Small | `text-sm` | 15px (0.9375rem) | 170% | 2% | ラベル / ナビ |
| Caption | `text-xs font-medium` | 13px (0.8125rem) | 140% | 2% | メタデータ / 補足 |
| Label (min) | `text-xxs font-medium` | 10px (0.625rem) | 140% | 2% | タブラベル / 最小ラベル（本文には使わない） |

---

## 行間・字間の設計（日本語組版の一般則から拡張）

CSSベースレイヤーで以下を適用:

```css
@layer base {
  body {
    line-height: 2.0;        /* 200% — 本文の可読性確保（デフォルト） */
    letter-spacing: 0.02em;  /* 2% — 日本語の自然な字間 */
  }
  h1, h2, h3, h4, h5, h6 {
    line-height: 1.4;        /* 140% — 見出しはコンパクトに */
    letter-spacing: 0.01em;  /* 1% — 見出しは控えめに */
  }
}
```

### ベースルール

- **body のデフォルト line-height は 2.0（200%）**: 18px 本文に対して行間を広くとることで、日本語の長文でもゆったりと読める。日本語 UI で一般に推奨される 150%〜175% のレンジより、さらに余白を重視して独自に拡張
- **見出しの line-height は 1.4（140%）**: 見出しはコンパクトに保ち、本文との視覚的コントラストを明確にする
- **フォームコントロールのラベル包含要素には `leading-normal`（150%）を付与する**: body の `line-height: 2.0` はインライン `<label>` / `<span>` に継承される。親要素の line-height がラインボックスの最小高さを決めるため、`text-sm` 等で個別に line-height を指定しても親の 2.0 が勝つ。ラジオボタン・チェックボックス・トグルスイッチのラベル + 説明テキストを包含する `<div>` には `leading-normal` を付与してリセットする
- CSSベースレイヤーで自動適用されるため、上記のフォームコントロール例外を除き個別の line-height 指定は原則不要

**設計の背景**: 行間（200%）と字間（2%）を広げることで文字の周りに空気が生まれ、ゆったりと見える。本文を18pxに引き上げたことで、見出し全体もスケールアップし、情報の階層がより明確になった。字間（見出し1%・本文2%）と見出しの行間（140%）は日本語 UI の一般的な推奨レンジに沿い、本文の行間は一般則（150%〜175%）を超えて200%に拡張している。

---

## フォントウェイト

| 用途 | クラス | ウェイト |
|------|--------|---------|
| 見出し | `font-bold` | 700 |
| セクション見出し | `font-semibold` | 600 |
| 本文 | `font-normal` | 400 |
| 強調 | `font-medium` | 500 |
| ラベル | `font-medium` | 500 |

---

## テキストカラー

| 用途 | Tailwindクラス | 値 | 備考 |
|------|---------------|-----|------|
| 見出し・強調 | `text-slate-900` | #0f172a | 青みがかった黒 |
| 本文（デフォルト） | `text-body` | #3d4b5f | slate-600〜700の中間 |
| 補助テキスト | `text-slate-500` | #64748b | ラベル・メタデータ |
| 反転テキスト | `text-white` | #ffffff | アクセントボタン上 |
| アクセントテキスト | `text-primary-500` | — | リンク等 |
| エラーテキスト | `text-red-500` | #ef4444 | バリデーションエラー |

---

## ルール

- **本文のベースは `text-base`（18px）**: ゆったり路線。行間200%・字間2%と合わせて、長時間利用でも疲れない可読性を確保
- **純黒テキスト禁止**: `text-black` は使わず `text-slate-900`（#0f172a）を使う。青みがかった黒で長時間利用でも目に優しい
- **行間は用途で使い分ける**: 見出し140%、本文200%。CSSベースレイヤーで自動適用されるため、個別指定は原則不要
- **字間は微量に広げる**: `tracking-tight` は禁止。見出し1%、本文2%を基本とする
- **見出しと本文のサイズ比は120%〜150%**: テキストの階層をサイズで明確にする

---

## 禁止パターン

> `foundations/prohibited.md`「タイポグラフィ」参照
